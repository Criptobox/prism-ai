# Lo que NO entra en la imagen.
#
# Ojo con los ZIP: `public/demo-sandbox.zip` y `public/demo-modulos.zip` son
# las demos que sirve el Sandbox. Un `*.zip` a secas aquí las dejaba fuera y
# el botón «Probar con una demo» daba 404 solo en Docker.
node_modules
.next
.git
.github
*.log
playwright-report
test-results
tests
