# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies — el lockfile del proyecto es package-lock.json
node_modules
bun.lock
bun.lockb
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/test-results/
/playwright-report/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/
# Prism AI — artefactos locales
/download/
/workspace/
/upload/
/tool-results/
/.zscripts/
/db/
/prism-ai-v*.zip

# ejemplo de entorno SÍ se versiona
!.env.example

# Los escribe `next dev` en cada arranque (generate-agent-files.js), no son
# código del proyecto. Mismo criterio que next-env.d.ts: fuera del control de
# versiones para que no aparezcan como cambios sueltos cada vez.
/AGENTS.md
/CLAUDE.md

# capturas de verificación local
tmp/
